# Sol

Drop project-specific assets in `assets/`.

Use these filenames in `project.js`:
- `assets/screenshot-1.png`
- `assets/screenshot-2.png`
- `assets/architecture.png`

You can rename them if you want, just update the `src` values in `project.js`.
